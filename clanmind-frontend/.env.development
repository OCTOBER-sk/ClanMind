# Local development defaults to the deterministic demo runtime so the full
# product works without the backend. Flip VITE_DEMO_MODE to 0 (and fill the
# live endpoints above) once integrating against a real deployment.
VITE_API_BASE_URL=/api/v1
VITE_DEMO_MODE=1
