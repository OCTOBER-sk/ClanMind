You are fixing the three defects found by the final audit of ClanMind backend. Source-of-truth: "ClanMind Backend - Master Implementation Specification.md" at repo root. Nothing else may drift.

DEFECT 1 (HIGH): apps/worker/src/ai/orchestrator.ts line ~371 hardcodes private_conversation_id: null when persisting the final AI message. Consequence: PRIVATE_AI / PRIVATE_PAIR replies are unreadable under RLS by anyone, so private chat cannot work end-to-end (spec sections 2.4, 11, 40, 55). ALSO: the client-supplied conversation id is never ownership-checked. FIX: resolve the correct private_conversation_id server-side from the run context (visibility + requester + group AI), verify requester membership in that conversation per section 40 before writing, and reject/cleanly fail runs whose claimed conversation they do not own. Add tests: PRIVATE_AI run persists message readable by owner via conversation ACL path; second user cannot read it; forged conversation id fails.

DEFECT 2 (MEDIUM): model router fallback - spec section 61 says never fall back silently on invalid API key / permission / safety refusal etc. Audit found both error branches break identically so fallback continues anyway (packages/domain model router / ai-providers fallback loop). FIX: classify-then-gate: NON_RETRYABLE classes (AUTH, INVALID_REQUEST, PERMISSION, SAFETY) must abort the chain, only TRANSIENT/RATE_LIMITED/PROVIDER_UNAVAILABLE proceed to next route. Tests: invalid-key stops chain (no second provider call), transient proceeds.

DEFECT 3 (MEDIUM): WS message.send in group-room.ts masks RATE_LIMITED / permission errors as VALIDATION_FAILED frames. FIX: map AppError codes through to error frames faithfully (RATE_LIMITED carries retry_after_seconds, GROUP_PERMISSION_DENIED etc per section 102). Test each mapping.

VERIFY LOOP after each fix: pnpm --filter @clanmind/worker typecheck && pnpm -r test - all green (262+ baseline grows). Then full self-review of your diff vs the three spec sections, then report: files changed, exact test totals, spec-section-by-spec-section confirmation.
