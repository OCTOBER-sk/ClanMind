You are fixing defects found by docs/BACKEND_DEEP_AUDIT.md (read it first - it has file:line evidence). Authority: backend spec at repo root. Fix ONLY these four:

DEFECT C1 (CRITICAL): apps/worker/src/repositories/ai-runtime.repo.ts line ~447 selects limit_value from quota_states but the migration defines limit_override. Every AI run start would throw on the real schema. FIX the query to match the actual migration column (read supabase/migrations to confirm exact name). ADD a regression test that validates the repo's SQL strings reference only columns that exist in the migrations (parse CREATE TABLE columns from migrations dir and cross-check SELECT/INSERT column lists of ai-runtime.repo.ts at minimum).

DEFECT H1 (HIGH): apps/worker/src/ai/runtime.ts line ~193 constructs new ContextEngine([], ...) - empty fixed slices mean no system safety text, no Odin identity, no group/project policy ever reach prompts; INJECTION_POLICY_TEXT exists but has zero production consumers. FIX per spec section 60: build the real fixed-slice set (system safety/platform policy incl INJECTION_POLICY_TEXT per section 89, Odin identity from ai_agents row, group policy from group settings, active project instructions via project_instructions table section 29, enabled skill instructions) and pass them into ContextEngine construction. Add test asserting assembled prompt contains safety policy + identity + project instruction when present.

DEFECT H2 (HIGH): reactions path skips group membership - packages/domain/src/messages/message.service.ts line ~158 early-returns GROUP rows without membership check; engagement handler never checks. Per spec sections 86/187: verify requester is an active member of the message's group before ANY reaction write (GROUP and private visibility both), removed members get denied. Add tests: non-member reaction rejected, removed-member rejected, cross-group rejected.

DEFECT H4 (HIGH): vacuous security tests - find the expect(true).toBe(true) placeholder in the security suite and replace it with a genuine assertion per its named scenario (spec section 187); upgrade boolean-only 55A row assertions where trivially possible to assert actual leakage-prevention behavior (zero rows / rejection), following existing test patterns.

VERIFY LOOP until green: pnpm -r typecheck && pnpm -r test (281 baseline grows). Self-review diff vs spec sections 29/60/86/89/187 + the audit evidence lines. Report files changed, exact totals, per-defect confirmation.
Do NOT touch clanmind-frontend/. Do NOT attempt H3 (sync endpoints - scheduled separately).
