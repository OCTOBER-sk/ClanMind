Agent: zeus - Backend remediation Task B3+C of H (handoff HANDOFF_BACKEND.md sections 3.B.5 and 3.C)
Repo workdir: clanmind-backend/. Sources: HANDOFF_BACKEND.md sections 3.B.5 and 3.C; spec sections 113 (GitHub endpoints), 80 (webhooks), 91 (rate limiting), 139 (branch safety), 140 (diff requirement).

CONTEXT: Tasks A, B1, B2 done. All five handler files except github.ts exist but are NOT yet mounted. Env vars GITHUB_WEBHOOK_SECRET etc already declared in env.ts. github.repo.ts provides connections findByGroup/findByInstallation/connect-upsert/disconnect + WebhookEventStore.beginDelivery durable dedupe. ApprovalEngine propose supports action_kind github.<type> risk HIGH requires_approval true; verifyWebhookSignature helper exists in packages/github (grep for it; if absent implement HMAC sha256 check inline).

DELIVERABLES:
1. apps/worker/src/handlers/github.ts - exactly per HANDOFF 3.B.5: connect/disconnect/status on /groups/:g/github/* (OWNER/ADMIN mutations, outbox github.connected/disconnected), GET /projects/:p/github/actions joining github_actions x ai_actions, POST actions {action_type create_branch|apply_patch|create_pr, branch_name, base_sha, head_sha, changed_files[]} with connection lookup, assertBranchSafety({branch_name, default_branch}), preview=buildDiffPreview, enforceRateLimit(gh:<groupId>, limits.github_actions_per_hour_per_group, 3600000), engine propose(action_kind github.<type>, HIGH, requires_approval true, payload incl preview) then INSERT github_actions row + outbox github.action.proposed. POST /api/v1/github/actions/:actionId/approve {displayed_payload_hash, displayed_payload_version} + reject - OWNER/ADMIN, engine double-checks HIGH risk; after approve engine.beginExecution then if no GitHub creds configured leave APPROVED and respond {executed:false, reason:"github_credentials_not_configured"} transparently.
2. Webhook route in same file: POST /api/v1/webhooks/github - raw text body required, secret = env.GITHUB_WEBHOOK_SECRET else 503, verifyWebhookSignature(raw, x-hub-signature-256 header, secret), delivery id from x-github-delivery header + event from x-github-event, webhookEvents.beginDelivery durable dedupe (duplicate delivery returns 200 idempotently), resolveGroupId via connections.findByInstallation(installation_id), persist payload row with group_id, outbox github.webhook.received.
3. Mount ALL FIVE routers (ai, ai-config, memory, intel, github) in apps/worker/src/app.ts under /api/v1 following existing mount style.
4. Task C: rate limit wiring in existing handlers/messages.ts POST - enforceRateLimit(msg:<user_id>, limits.messages_per_minute_per_user, 60000) before create, limits parsed via c.env like other handlers.
5. Tests in apps/worker/test/: webhook signature reject (bad signature 401), durable dedupe (same delivery id twice processed once), approvals approve/reject flow + expired action rejection (stale hash rejected), branch safety rejects default branch writes, messages rate limit 429.

RULES: zod validation, thin handlers, no new deps, raw body capture for webhook must work on Cloudflare Workers (await c.req.text() before json parse). pnpm --filter @clanmind/worker typecheck && pnpm -r test until green.

FINAL SELF-REVIEW: git diff review, scope check (app.ts mount lines + github.ts + messages.ts + tests only), both commands green, report files changed + test count + deviations incl which WS choice you made nowhere (not this task).
